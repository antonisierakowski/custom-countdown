import React from 'react'
import styled from 'styled-components';

const Cake = props => {
    return (
        <CakeProvider>

        </CakeProvider>
    )
}

const CakeProvider = styled.div`

`

export default Cake