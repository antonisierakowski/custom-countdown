import { Button } from '@material-ui/core'
import styled from 'styled-components'

const CreateCountdownButton = styled(Button)`
    &&& {
        font-family: inherit;
    }
`

export default CreateCountdownButton